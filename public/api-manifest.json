{
  "schema_version": "1.1",
  "site": "https://resume.sam-rogers.com/",
  "description": "Public UI endpoints used by the Sam Rogers AI-queryable resume.",
  "privacy": {
    "summary": "The fit assessment and chat endpoints receive user-supplied text for Anthropic analysis. Submitted text is not intentionally stored by this app. Users should not paste confidential, proprietary, regulated, or unreleased role data; they should email sensitive roles to sam@sam-rogers.com instead.",
    "contact": "mailto:sam@sam-rogers.com"
  },
  "endpoints": [
    {
      "path": "/api/chat",
      "method": "POST",
      "purpose": "Answer user questions about Sam Rogers' resume, evidence, gaps, portfolio, and role fit.",
      "authentication": "none",
      "rate_limit": {
        "enabled": "conditional",
        "provider": "Upstash Redis",
        "required_env": [
          "UPSTASH_REDIS_REST_URL",
          "UPSTASH_REDIS_REST_TOKEN"
        ],
        "limits": [
          {
            "window": "60 s",
            "max_requests": 5
          },
          {
            "window": "1 h",
            "max_requests": 50
          }
        ],
        "missing_config_behavior": "fail_open_local_dev_fail_closed_production"
      },
      "intended_client": "interactive resume UI"
    },
    {
      "path": "/api/analyze-fit",
      "method": "POST",
      "purpose": "Compare a pasted job description against Sam Rogers' profile and return an honest role-fit assessment.",
      "authentication": "none",
      "rate_limit": {
        "enabled": "conditional",
        "provider": "Upstash Redis",
        "required_env": [
          "UPSTASH_REDIS_REST_URL",
          "UPSTASH_REDIS_REST_TOKEN"
        ],
        "limits": [
          {
            "window": "1 h",
            "max_requests": 10
          }
        ],
        "missing_config_behavior": "fail_open_local_dev_fail_closed_production"
      },
      "intended_client": "interactive resume UI"
    }
  ],
  "openapi": {
    "provided": false,
    "reason": "These endpoints support the website UI rather than a public integration API."
  }
}
